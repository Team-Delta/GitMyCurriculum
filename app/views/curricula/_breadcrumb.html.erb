<% @path.each do |p| %>
  <% if @counter > 1 %>
    <li>
      <%= link_to "#{p[:name]}", p[:path], :remote => true %>
    </li>
  <% else %>
    <li>
      <%= link_to "#{p[:name]}", p[:path] %>
    </li>
  <% end %>
  <% @counter = @counter + 1 %>
<% end %>